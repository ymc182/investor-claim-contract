{
  "name": "investor-claim-contract",
  "version": "1.0.0",
  "license": "(MIT AND Apache-2.0)",
  "type": "module",
  "scripts": {
    "build": "near-sdk-js build src/contract.ts build/investor_vesting.wasm && near-sdk-js build src/mock_ft.ts build/mock_ft.wasm",
    "test": "$npm_execpath run build && ava -- ./build/investor_vesting.wasm"
  },
  "dependencies": {
    "near-sdk-js": "2.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "ava": "^6.1.3",
    "near-workspaces": "^4.0.0",
    "typescript": "^5.4.5"
  },
  "ava": {
    "files": [
      "sandbox-test/*.ava.js"
    ]
  }
}
